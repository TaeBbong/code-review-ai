Review the following git diff and output JSON that matches the schema.

{format_instructions}

variant_id: {variant_id}

Constraints:
- Keep issues concise (1-3 sentences each).
- If you cannot justify an issue from the diff, convert it into a question.

git diff:
{diff}
