id: g2-iterative
description: "Iterative refinement: initial review + self-validation to reduce false positives"
templates:
  review_system: review.system.txt
  review_user: review.user.txt
  repair_system: repair.system.txt
  repair_user: repair.user.txt
params:
  bad_max_chars: 4000
