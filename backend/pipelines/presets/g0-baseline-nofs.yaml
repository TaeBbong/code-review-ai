id: g0-baseline-nofs
pipeline: backend.pipelines.variants.g0_baseline:BaselinePipeline
prompt_pack: g0-baseline-nofs
params:
  diff_source: staged # raw | staged | worktree | range
  context_lines: 3
  bad_max_chars: 4000
