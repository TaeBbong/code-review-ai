id: g1-mapreduce
pipeline: backend.pipelines.variants.g1_mapreduce:MapReducePipeline
params:
  diff_source: staged
  context_lines: 3
  max_chars: 400000
  # map-reduce 설정
  min_files_for_split: 2
  max_files_for_split: 20
  max_concurrency: 4
  # evidence 설정
  max_symbols: 12
  top_k_per_symbol: 6
  bad_max_chars: 4000
